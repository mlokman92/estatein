# dependencies
node_modules

# build output
dist
dist-ssr

# local env (never commit real keys)
.env
.env.local
.env.*.local
# ...but do commit the template (overrides the root .env* rule)
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.DS_Store
*.tsbuildinfo
.vite
